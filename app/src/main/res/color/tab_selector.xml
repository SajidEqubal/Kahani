<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@android:color/white" android:state_selected="true" /> <!-- Selected state -->
    <item android:color="@android:color/white" android:state_pressed="true" />  <!-- Pressed state -->
    <item android:color="@android:color/holo_orange_light" />                   <!-- Default (unselected) -->
</selector>
